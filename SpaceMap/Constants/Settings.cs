namespace IngameScript
{
    public static class Settings
    {
        public const string NameTag = "[SM]";

        public const string DisplayNameRegex = @"\[SM(?::([a-zA-Z0-5\-\;]*))?\]";
    }
}